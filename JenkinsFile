pipeline{
    agent any
    environment{
        staging_server = "13.235.242.139:8080"
    }

    stages{
        stage('Deploy to remote'){
            steps{
                ssh ' -r scp ${WORKSPACE}/* root@${staging_server}:/var/wwww/html/demowebsite/'
            }
        }
    }
}
